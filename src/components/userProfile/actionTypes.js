import { NAME } from './constants';
/**
 * Fired by the {@link actions.getUserAuthJwt getUserAuthJwt}
 * action creator.
 *
 * @type {String}
 */
export const REQUEST_AUTH_USER_JWT = `${NAME}/REQUEST_AUTH_USER_JWT`;
