import { NAME } from './constants';

/**
 * Fired by the {@link actions.setUserData setUserData}
 * action creator.
 *
 * @type {String}
 */
export const REMOVE_ITEM_FROM_CART = `${NAME}/REMOVE_ITEM_FROM_CART`;
